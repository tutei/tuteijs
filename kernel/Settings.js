var Settings = {

    /**
     * Determines how long the current frame is displayed.
     * If you don't want to limit the frame rate set it to 0
     * <p>
     * It will keep the app running at a specific frame rate.
     * <p>
     * 40 ms implies 25fps (1000/40) = 25
     */
    FRAME_DELAY:0,
    physicsScale:20,
    tileWidth:20,
    tileHeight:20,
    screenColor:'#fff',
    showFPS:true,

    SPRITE_SPEED:1,

    walkDelay:100,
	
	inputPreventDefault:true
    

};
